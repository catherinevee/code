apiVersion: v1
kind: Service
metadata:
  name: {{ include "starrez-chart.fullname" . }}
  labels:
    {{- include "starrez-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starrez-chart.selectorLabels" . | nindent 4 }}
