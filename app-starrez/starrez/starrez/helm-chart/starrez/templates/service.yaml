apiVersion: v1
kind: Service
metadata:
  name: {{ include "starrez.fullname" . }}
  labels:
    {{- include "starrez.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starrez.selectorLabels" . | nindent 4 }}
